# THIS PROGRAM FOR SHIFT CONTROL 
IF("CHANGE" .EQ. OFF .AND. "FLAG" .EQ. OFF) THEN GOTO 5
IF("CHANGE" .EQ. ON) THEN OFF("FLAG")
GOTO 8
"SHIFT" = "SHIFT" + 1
IF("SHIFT" .GT. 2) THEN SET(1,"SHIFT")
ON("CHANGE")
# SHIFT VALUE CHANGE WHEN YOU CHANGE CONDITION OF CHANGE ON TO OFF
